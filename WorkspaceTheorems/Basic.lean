-- 基础定义和辅助定理

import Mathlib.Tactic

-- 示例定理 1: 简单数论
theorem example_nat_le (n : ℕ) (h : n > 0) : n ≥ 1 := by
  sorry

-- 示例定理 2: 算术
theorem example_add (a b : ℕ) : a + b = b + a := by
  sorry

-- 用户定理将动态添加到此文件
